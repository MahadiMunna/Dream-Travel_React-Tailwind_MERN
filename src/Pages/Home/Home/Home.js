import React from 'react';
import useTitle from '../../../Hooks/useTitle';

const Home = () => {
    useTitle('Home');
    return (
        <div>
            
        </div>
    );
};

export default Home;