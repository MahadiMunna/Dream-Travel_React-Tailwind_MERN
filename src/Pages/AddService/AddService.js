import React from 'react';
import useTitle from '../../Hooks/useTitle';

const AddService = () => {
    useTitle('Service');
    return (
        <div>
            
        </div>
    );
};

export default AddService;