import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Reviews = () => {
    useTitle('Review')
    return (
        <div>
            
        </div>
    );
};

export default Reviews;